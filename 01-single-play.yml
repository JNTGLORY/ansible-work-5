#  ansible n1 -m file -a "path=/home/ec2-user/ni_dir state=directory"
---

- name: Update n1 webservers
  hosts: n1

  tasks:
  - name: Create Directory
    file: 
      path: /home/ec2-user/new_dir
      state: directory
...        
